# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Vision-LLM OCR (reads the broadcast score bug). Gemini 2.5 Flash Lite.
OPENROUTER_API_KEY=sk-or-v1-...

# Optional. Only needed to run the live TxLINE integration tests, which are
# skipped without them. See scripts/probe-stat-validation.ts for how to mint them.
SIUUU_JWT=
SIUUU_API_TOKEN=
