node_modules/
.next/
.DS_Store
._*
dist/

# 91MB captured corpus — test data, deliberately not in git.
# Untracked is not enough: one `git add -A` would stage it.
exact-match-txline-raw/

# Secrets — never commit
.env
.env.*
!.env.example
